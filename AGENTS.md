# Workflow sản phẩm

- Với người mới vào team, người chưa rõ dự án ở đâu hoặc chưa biết nên làm gì tiếp: đọc `skill://project-guide` trước để định hướng.
- Với yêu cầu bắt đầu/tiếp tục dự án, phân tích nghiệp vụ, stories/UX, kiến trúc, backlog/sprint, bàn giao hoặc ma trận tiến độ: đọc `skill://product-workflow`.
- Nếu phiên chưa khám phá skills mới, đọc trực tiếp file trong `.agents/skills/`; không cài thêm bộ workflow khác hoặc sửa settings để khắc phục tạm.
- `product-workflow` chọn chuyên gia cần thiết. Không nạp cả bộ cho mọi câu hỏi hoặc bắt lỗi nhỏ đi lại toàn bộ quy trình.
- Giữ nguyên nguồn tài liệu đã có; không tự coi nội dung là đã được duyệt, không tạo hồ sơ/checkpoint ngoài yêu cầu lưu của người dùng.
- Jira chưa được bộ skills này kết nối. Không báo đã claim/publish/Done trên Jira khi thiếu công cụ, quyền và kết quả xác nhận.
- Hướng dẫn workflow không thay thế công cụ thực thi hay quyền của người dùng. Không thực hiện external writes/deploy từ yêu cầu chỉ xem hoặc thiết kế.
